# Hide-Taskbar-Only-on-Desktop-Windhawk-Mod
Hides the Windows taskbar whenever there's nothing else to show (desktop focused, or every window minimized), and shows it again immediately when any other window is focused.
